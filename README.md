As it uses some preview features ( as of now as it uses jdk 21 ) please follow the steps: 

In terminal execute :
javac --release 21 --enable-preview <file_name>.java
java --enable-preview <file_name>  
